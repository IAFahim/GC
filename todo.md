# TODO

1. Make append to clipboard default.
2. Fix extra whitespace at the end of each file.
3. Implement `--exclude-line-if-start` argument (takes a list of strings). Discard lines starting with these strings (e.g. `//`, `///`, `\n`).
4. Build, test, QA, push, action check.
