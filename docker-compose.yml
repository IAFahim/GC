services:
  gc:
    build:
      context: .
      dockerfile: Dockerfile
    image: gc
    volumes:
      - .:/workspace
    working_dir: /workspace

  gc-test:
    image: mcr.microsoft.com/dotnet/sdk:10.0
    volumes:
      - .:/workspace
    working_dir: /workspace
    command: ["dotnet", "test", "tests/gc.Tests/gc.Tests.csproj"]

  gc-bash:
    image: mcr.microsoft.com/dotnet/sdk:10.0
    volumes:
      - .:/workspace
    working_dir: /workspace
    entrypoint: ["/bin/bash"]
    stdin_open: true
    tty: true
